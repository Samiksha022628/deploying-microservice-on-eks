serviceAccount:
  create: true
  name: fluent-bit-${ENV}
  annotations: {} 

cloudWatch:
  enabled: true
  logGroupName: /eks/${ENV}/app-logs 
  region: us-east-1                
  autoCreateGroup: true

tolerations:
  - key: node-role.kubernetes.io/control-plane
    effect: NoSchedule
